/* export const ToggleTabs = (type, tab) => {
    return {
        type: type,
        tab: tab
    }
} */

export const ToggleMenu = (type, state) => {
    return {
        type: type,
        state: state
    }
}